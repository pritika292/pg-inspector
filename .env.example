# Copy to .env and fill in real values for local development.
# .env is gitignored; .env.example is committed.

# Port the server listens on
PORT=3014

# Environment label: development | test | production
NODE_ENV=development

# Read-only Postgres role used by the HTTP server. Local dev uses port 5433
# (5432 is taken by shortlive's local Postgres on the same machine).
DATABASE_URL=postgres://inspector:devpass@localhost:5433/pg_scenarios

# Admin Postgres role used ONLY by the boot-time migrator + seeder.
# The HTTP server never reads this — separation enforced in src/server/db/.
# In local dev this is the same DSN; on prod it's the inspector_admin role.
ADMIN_DATABASE_URL=postgres://inspector:devpass@localhost:5433/pg_scenarios

# Redis logical DB 13 (port 6380 locally — 6379 is shortlive's)
REDIS_URL=redis://localhost:6380/13

# Azure OpenAI for NL→SQL, EXPLAIN-AI, and advise. No API key — auth is via
# DefaultAzureCredential (Managed Identity on prod, az-login user on local).
# Locally you need an `az login` whose principal has the "Cognitive Services
# OpenAI User" role on the AI resource.
AZURE_OPENAI_ENDPOINT=https://pritika-ai.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT=gpt-4.1-mini
AZURE_OPENAI_API_VERSION=2024-12-01-preview

# Logging
LOG_LEVEL=info
