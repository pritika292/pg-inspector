import { describe, expect, it } from "vitest";

// Placeholder until the real server lands in #10.
describe("scaffold", () => {
  it("vitest workspace is wired", () => {
    expect(true).toBe(true);
  });
});
